-- Migration to add trading statistics columns
-- These columns were already added in migration 008, so this migration is not needed
-- Migration file kept for tracking purposes but commands removed to avoid duplicate column errors
SELECT 1;